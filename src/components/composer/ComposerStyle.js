import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  composer: {
    position: 'relative',
    width: '100%',
    height: '100%',
    padding: 0,
    margin: 0,
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },

  container: {
    width: '80%',
    height: '100%'
  }
});